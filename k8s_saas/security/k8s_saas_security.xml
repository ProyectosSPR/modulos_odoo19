<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Module Category -->
    <record id="module_category_k8s_saas" model="ir.module.category">
        <field name="name">Kubernetes SaaS</field>
        <field name="description">Gestión de instancias Odoo en Kubernetes</field>
        <field name="sequence">50</field>
    </record>

    <!-- User Group -->
    <record id="group_k8s_user" model="res.groups">
        <field name="name">User</field>
        <field name="category_id" ref="module_category_k8s_saas"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Manager Group -->
    <record id="group_k8s_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="module_category_k8s_saas"/>
        <field name="implied_ids" eval="[(4, ref('group_k8s_user'))]"/>
        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

    <!-- Record Rules -->

    <!-- Clusters: Only managers can access -->
    <record id="k8s_cluster_rule_manager" model="ir.rule">
        <field name="name">K8s Clusters: Manager Access</field>
        <field name="model_id" ref="model_k8s_cluster"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_k8s_manager'))]"/>
    </record>

    <!-- Instances: Users can view, managers can edit -->
    <record id="k8s_instance_rule_user" model="ir.rule">
        <field name="name">K8s Instances: User Read</field>
        <field name="model_id" ref="model_k8s_instance"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_k8s_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="k8s_instance_rule_manager" model="ir.rule">
        <field name="name">K8s Instances: Manager Full Access</field>
        <field name="model_id" ref="model_k8s_instance"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_k8s_manager'))]"/>
    </record>

</odoo>
